# SDD Tasks — JS to TS Migration

- [ ] Task 1: Client dependency setup
  - Install `typescript`, `@types/react`, `@types/react-dom` in `client/`.
- [ ] Task 2: Client TS configurations
  - Create `client/tsconfig.json` and `client/tsconfig.node.json`.
- [ ] Task 3: Client code conversion
  - Create `client/src/types.ts` with definitions for `Player` and `RoomState`.
  - Rename and refactor `client/src/socket.js` -> `client/src/socket.ts`.
  - Rename and refactor `client/src/screens/*.jsx` -> `client/src/screens/*.tsx`.
  - Rename and refactor `client/src/App.jsx` -> `client/src/App.tsx`.
  - Rename and refactor `client/src/main.jsx` -> `client/src/main.tsx`.
  - Rename and refactor `client/vite.config.js` -> `client/vite.config.ts`.
  - Update `client/index.html` referencing script path to `src/main.tsx`.
- [ ] Task 4: Server dependency setup
  - Install `typescript`, `tsx`, `@types/express`, `@types/node` in `server/`.
- [ ] Task 5: Server TS configurations
  - Create `server/tsconfig.json`.
- [ ] Task 6: Server code conversion
  - Create `server/src/types.ts`.
  - Rename and refactor `server/src/questions.js` -> `server/src/questions.ts`.
  - Rename and refactor `server/src/rooms.js` -> `server/src/rooms.ts`.
  - Rename and refactor `server/src/socket.js` -> `server/src/socket.ts`.
  - Rename and refactor `server/src/index.js` -> `server/src/index.ts`.
- [ ] Task 7: Build scripts & Dockerfile update
  - Update `server/package.json` scripts (`build`, `dev`).
  - Update root `package.json` scripts.
  - Update `Dockerfile` to compile the server and run from `server/dist/index.js`.
- [ ] Task 8: Verification & Compilation test
  - Run type checks and build scripts to ensure a zero-error output.
